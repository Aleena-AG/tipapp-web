import SPReviewsContainer from "@/components/organisms/service-provider/SP-ReviewsContainer/SPReviewsContainer";
import SPSubScreenLayout from "@/layout/SPSubScreenLayout";

const SPReviewsScreen = () => {
  return (
    <SPSubScreenLayout maxWidth="md">
      <SPReviewsContainer />
    </SPSubScreenLayout>
  );
};

export default SPReviewsScreen;
