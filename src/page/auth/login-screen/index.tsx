import LoginContainer from "@/components/organisms/auth/loginContainer/loginContainer";

const LoginScreen = () => {
  return (
    <div className="bg-white flex-1 flex flex-col min-h-screen">
      <LoginContainer />
    </div>
  );
};

export default LoginScreen;
